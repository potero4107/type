
const { blackA } = require('@radix-ui/colors')

import defaultTheme from "@nuxtjs/tailwindcss"
export default {
    theme: {
      extend: {
        colors: {
        
        },
        fontSize: {
        },
       fontFamily: {
        // title:'Rockwell Std'
        } 
      }
    }
  }
